@inherits BGDialogBase

<BGModal ref="BGModalRef">

    <div class="bg-dialog-outer-div" style="@GetSizeStyles()">

        <div class="bg-dialog-inner-div">

            <div class="bg-dialog-popup-titlebar">
                <span class="bg-dialog-popup-titlebar-text">
                    @DialogTitle
                </span>
            </div>

            <div class="bg-dialog-child-div">
                @ChildContent
            </div>

            <div class="bg-dialog-button-div">
                @if ((ButtonSelection == ButtonStyles.OK) || (ButtonSelection == ButtonStyles.OKCancel)) {
                    <BGButton ButtonId="@BGButton.kStdId_OK" ButtonText="OK" OnClick="OnClickComplete" />
                }

                @if (ButtonSelection == ButtonStyles.YesNo) {
                    <BGButton ButtonId="@BGButton.kStdId_Yes" ButtonText="Yes" OnClick="OnClickComplete" />
                }

                <!-- We'll generate custom buttons here, if we have any -->
                @if (CustButtons != null)
                { 
                    foreach (KeyValuePair<string, string> custButt in CustButtons)
                    {
                        <BGButton ButtonId="@custButt.Key" ButtonText="@custButt.Value" OnClick="OnClickComplete" />
                    }
                }

                @if (ButtonSelection == ButtonStyles.OKCancel) {
                    <BGButton ButtonId="@BGButton.kStdId_Cancel" ButtonText="Cancel" OnClick="OnClickComplete" />
                }

                @if (ButtonSelection == ButtonStyles.YesNo) {
                    <BGButton ButtonId="@BGButton.kStdId_No" ButtonText="No" OnClick="OnClickComplete" />
                }
            </div>
        </div>
    </div>

</BGModal>
