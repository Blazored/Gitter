@inherits RoomSendBase

<div class="chat-room__filter">
	<button class="chat-room__filter-button@(MessageFilter.FilterUnread ? "--active" : "")" onclick="@FilterUnread">Unread</button>
	@if (!string.IsNullOrWhiteSpace(MessageFilter.FilterByUserId))
	{
		<button class="chat-room__filter-button--active" onclick="@RemoveUserFilter">Clear User Filter</button>
	}
</div>
<form onsubmit="@SendMessage" class="chat-room__send-message-form">
	<img src="@User.AvatarUrlSmall" width="38" height="38" class="chat-room__send-message-avatar" />
	<textarea id="messageinput"
						class="@NewMessageClass"
						bind="@NewMessage"
						oninput="@HandleSizing"
						placeholder="Join the conversation..." />
	<button type="submit" class="chat-room__send-button">Ok</button>
</form>
